{
  "name": "chatta",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "engines": { "node": ">=18" }
}
